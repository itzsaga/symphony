{
  "name": "symphony",
  "version": "0.0.0",
  "description": "Symphony v1 — Effect.ts + Claude Code orchestrator (per .claude/prds/symphony-v1/PRD.md).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/main.ts ./WORKFLOW.md",
    "start": "bun run src/main.ts",
    "test": "bun test",
    "test:integration": "bun test test/integration/",
    "typecheck": "tsc --noEmit",
    "audit:section-17": "bun run scripts/audit-section-17.ts"
  },
  "dependencies": {
    "@effect/platform": "^0.96.1",
    "@effect/platform-bun": "^0.89.0",
    "@effect/schema": "^0.75.5",
    "effect": "^3.21.2",
    "liquidjs": "^10.25.7"
  },
  "devDependencies": {
    "@effect/vitest": "^0.29.0",
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.0"
  }
}
